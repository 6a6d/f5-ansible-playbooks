- name: Provision LTM
  bigip_provision:
    module: ltm
    level: nominal
    provider: "{{ login_info }}"
  delegate_to: localhost

- name: Provision AVR
  bigip_provision:
    module: avr
    level: nominal
    provider: "{{ login_info }}"
  delegate_to: localhost