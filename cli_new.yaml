vars:
  cli:
    host: bigipa
    username: admin
    password: admin
    transport: cli

name: run show version on remote devices
  bigip_command:
    commands: show sys version
    provider: "{{ cli }}"